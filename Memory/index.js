const memory = require("./libs/build/Release/memory.node");

console.log(Object.keys(memory));

/**
 *
 * @param {*} b
 * @param {*} c
 * @param {*} d
 * @returns
 */
function a(b, c, d) {
  return 0;
}
